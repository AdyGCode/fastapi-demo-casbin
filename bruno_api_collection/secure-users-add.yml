info:
  name: /secure/users/add
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/secure/users/add
  headers:
    - name: x-username
      value: jane
    - name: x-password
      value: secret123
  body:
    type: json
    data: |-
      {
        "user": {
          "name": "ivor",
          "password": "pass123"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

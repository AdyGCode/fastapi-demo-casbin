info:
  name: /insecure
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/insecure
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
